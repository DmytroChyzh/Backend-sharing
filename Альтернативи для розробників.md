# Промпт для Cursor: Модифікація слайду #16 з Backend Ecosystem анімацією

## 🎯 Мета
Замінити статичний контент слайду #16 (Аналоги бекендів) на інтерактивну Web3-стиль анімацію з літаючими логотипами та центральною карткою з детальною інформацією.

---

## 📋 ВАЖЛИВО: Перед початком реалізації

**DO PLAN FIRST!**

Перед тим як писати код, створи детальний план:

1. **Аналіз поточної структури:**
   - Який файл містить слайд #16?
   - Яка поточна структура HTML/React компоненту?
   - Які стилі вже існують?
   - Чи використовується CSS-in-JS, Tailwind, або окремі CSS файли?

2. **План інтеграції:**
   - Як буде організований новий код (окремий компонент чи вбудований)?
   - Які залежності потрібні (якщо є)?
   - Чи потрібно створювати окремі файли для анімації?
   - Як уникнути конфліктів зі існуючими стилями?

3. **План тестування:**
   - Як перевірити що анімація працює?
   - Чи потрібні fallback для слабких пристроїв?
   - Як буде виглядати на різних розмірах екрану?

4. **Backward compatibility:**
   - Чи можна легко повернутись до старої версії?
   - Чи потрібно зберегти старий контент як fallback?

**Тільки після створення плану - починай кодити!**

---

## 🎨 Дизайн концепція

### Загальний вигляд
```
┌─────────────────────────────────────────────────┐
│                                                 │
│     🌌 BACKEND ECOSYSTEM (заголовок)           │
│                                                 │
│  🐘        📦           🏗️         ☁️          │
│    (логотипи літають по всьому екрану)         │
│         ┌──────────────┐                       │
│    🚂   │              │   🎨                  │
│         │ DETAIL CARD  │                       │
│         │  (центр)     │                       │
│  🗃️     │              │        🎯             │
│         └──────────────┘                       │
│                                                 │
│  📱                            🔵               │
│                                                 │
│         [Маленька CTA картка справа внизу]     │
└─────────────────────────────────────────────────┘
```

---

## 📐 Технічні специфікації

### 1. HTML Структура

```html
<div class="slide-16-container">
  <!-- Animated grid background -->
  <div class="grid-background"></div>
  
  <!-- Header -->
  <div class="header">
    <h1 class="header-title">🌌 BACKEND ECOSYSTEM</h1>
  </div>

  <!-- Main content area -->
  <div class="animation-container">
    <!-- Ambient glow around card -->
    <div class="card-ambient-glow" id="ambientGlow"></div>
    
    <!-- Central detail card -->
    <div class="detail-card" id="detailCard">
      <div class="card-content" id="cardContent">
        <div class="card-logo" id="cardLogo">🐘</div>
        <h2 class="card-title" id="cardTitle">SUPABASE</h2>
        <div class="card-divider"></div>
        <p class="card-description" id="cardDescription">...</p>
        <ul class="card-features" id="cardFeatures">...</ul>
        <div class="card-tags" id="cardTags">...</div>
        <div class="card-stats">...</div>
      </div>
    </div>

    <!-- Floating logos container -->
    <div class="logo-container" id="logoContainer"></div>
  </div>

  <!-- Small CTA card -->
  <div class="cta-section">
    <h2>❓ ХТОСЬ ЮЗАВ ЦІ ТУЛИ?</h2>
    <p>Поділіться досвідом!</p>
  </div>
</div>
```

---

## 🎨 CSS Стилі

### Колірна палітра
```css
:root {
  --bg-primary: #0a0a0f;
  --bg-secondary: #121218;
  --glass-bg: rgba(255, 255, 255, 0.05);
  --glass-border: rgba(255, 255, 255, 0.1);
  
  /* Backend colors */
  --supabase: #3ECF8E;
  --pocketbase: #0090FF;
  --appwrite: #FD366E;
  --aws: #FF9900;
  --railway: #8B5CF6;
  --render: #6366F1;
  --xata: #8B5CF6;
  --nhost: #0052CC;
  --back4app: #0080FF;
  --parse: #169CEE;
}
```

### Основні стилі

**Container:**
```css
.slide-16-container {
  position: relative;
  width: 100%;
  height: 100vh;
  overflow: hidden;
  background: linear-gradient(135deg, var(--bg-primary) 0%, var(--bg-secondary) 100%);
  color: #fff;
}
```

**Animated grid background:**
```css
.grid-background {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-image: 
    linear-gradient(rgba(255,255,255,0.03) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255,255,255,0.03) 1px, transparent 1px);
  background-size: 50px 50px;
  animation: gridMove 20s linear infinite;
  pointer-events: none;
}

@keyframes gridMove {
  0% { background-position: 0 0; }
  100% { background-position: 50px 50px; }
}
```

**Header:**
```css
.header {
  position: absolute;
  top: 60px;
  left: 50%;
  transform: translateX(-50%);
  text-align: center;
  z-index: 100;
}

.header-title {
  font-size: 3.5rem;
  font-weight: 800;
  background: linear-gradient(135deg, #00F5FF 0%, #B794F6 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  text-shadow: 0 0 40px rgba(0, 245, 255, 0.3);
  letter-spacing: 2px;
  animation: titleGlow 3s ease-in-out infinite;
}

@keyframes titleGlow {
  0%, 100% { filter: drop-shadow(0 0 20px rgba(0, 245, 255, 0.3)); }
  50% { filter: drop-shadow(0 0 40px rgba(0, 245, 255, 0.6)); }
}
```

**Detail Card (центральна):**
```css
.detail-card {
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  width: 420px;
  padding: 40px;
  background: var(--glass-bg);
  backdrop-filter: blur(20px);
  border: 2px solid var(--glass-border);
  border-radius: 24px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
  z-index: 50;
  transition: border-color 0.5s ease, box-shadow 0.5s ease;
}

.card-ambient-glow {
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  width: 600px;
  height: 600px;
  border-radius: 50%;
  pointer-events: none;
  z-index: 49;
  animation: ambientPulse 4s ease-in-out infinite;
  transition: background 0.5s ease;
}

@keyframes ambientPulse {
  0%, 100% { 
    opacity: 0.3; 
    transform: translate(-50%, -50%) scale(1); 
  }
  50% { 
    opacity: 0.6; 
    transform: translate(-50%, -50%) scale(1.1); 
  }
}

.card-logo {
  width: 100px;
  height: 100px;
  margin: 0 auto 20px;
  font-size: 80px;
  text-align: center;
  line-height: 100px;
}

.card-title {
  font-size: 2rem;
  font-weight: 700;
  text-align: center;
  margin-bottom: 10px;
  background: linear-gradient(135deg, #fff 0%, #E0E0E0 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.card-description {
  text-align: center;
  color: #E0E0E0;
  font-size: 1rem;
  line-height: 1.6;
  margin-bottom: 25px;
  min-height: 75px;
}

.card-features {
  list-style: none;
  margin-bottom: 20px;
}

.card-features li {
  padding: 8px 0;
  color: #E0E0E0;
  font-size: 0.95rem;
  display: flex;
  align-items: center;
  gap: 10px;
}

.card-features li::before {
  content: '→';
  color: inherit;
  font-weight: bold;
}

.card-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-bottom: 20px;
  justify-content: center;
}

.tag {
  padding: 6px 12px;
  background: rgba(255, 255, 255, 0.08);
  border: 1px solid rgba(255, 255, 255, 0.15);
  border-radius: 12px;
  font-size: 0.85rem;
  color: #E0E0E0;
}

.card-stats {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 12px;
  font-size: 0.9rem;
  color: #E0E0E0;
  text-align: center;
}
```

**Floating Logos:**
```css
.logo-container {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
}

.floating-logo {
  position: absolute;
  width: 70px;
  height: 70px;
  font-size: 56px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.3s ease;
  cursor: pointer;
  pointer-events: auto;
  filter: drop-shadow(0 0 20px currentColor);
}

.floating-logo.dimmed {
  opacity: 0.3;
  filter: drop-shadow(0 0 5px currentColor);
}

.floating-logo.active {
  filter: drop-shadow(0 0 40px currentColor);
  transform: scale(1.2);
}
```

**Trail & Effects:**
```css
.trail-particle {
  position: absolute;
  pointer-events: none;
  font-size: 40px;
  animation: trailFade 0.5s ease-out forwards;
}

@keyframes trailFade {
  to {
    opacity: 0;
    transform: scale(0.5);
  }
}

.ripple {
  position: absolute;
  border: 2px solid currentColor;
  border-radius: 50%;
  pointer-events: none;
  animation: rippleExpand 0.6s ease-out forwards;
}

@keyframes rippleExpand {
  from {
    width: 0;
    height: 0;
    opacity: 1;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
  }
  to {
    width: 300px;
    height: 300px;
    opacity: 0;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
  }
}

.burst-particle {
  position: absolute;
  width: 6px;
  height: 6px;
  border-radius: 50%;
  pointer-events: none;
  animation: burstFade 0.8s ease-out forwards;
}

@keyframes burstFade {
  to {
    opacity: 0;
    transform: translate(var(--tx), var(--ty)) scale(0);
  }
}
```

**CTA Section:**
```css
.cta-section {
  position: absolute;
  bottom: 40px;
  right: 40px;
  text-align: center;
  z-index: 100;
  padding: 16px 24px;
  background: var(--glass-bg);
  backdrop-filter: blur(10px);
  border: 1px solid var(--glass-border);
  border-radius: 12px;
  opacity: 0;
  animation: ctaFadeIn 1s ease-out 2s forwards;
}

@keyframes ctaFadeIn {
  to { opacity: 1; }
}

.cta-section h2 {
  font-size: 1rem;
  margin-bottom: 8px;
  background: linear-gradient(135deg, #00F5FF 0%, #B794F6 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.cta-section p {
  color: #E0E0E0;
  font-size: 0.85rem;
  line-height: 1.4;
}
```

**Content transitions:**
```css
.card-content {
  animation: contentSlide 0.5s ease-out;
}

@keyframes contentSlide {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

**Responsive:**
```css
@media (max-width: 768px) {
  .header-title { font-size: 2rem; }
  .detail-card { width: 90%; padding: 30px; }
  .floating-logo { width: 50px; height: 50px; font-size: 40px; }
  .cta-section { display: none; }
}
```

---

## 💻 JavaScript логіка

### 1. Data Structure - 10 бекендів

```javascript
const backends = [
  {
    name: 'Supabase',
    emoji: '🐘',
    color: '#3ECF8E',
    description: 'Open-source Firebase з PostgreSQL всередині. Realtime + auth з коробки.',
    features: [
      'PostgreSQL database',
      'Realtime subscriptions',
      'Built-in authentication'
    ],
    tags: ['#OpenSource', '#PostgreSQL', '#Realtime'],
    stats: ['⭐ 60k+ Stars', '💰 Free tier', '🌐 supabase.com', '📦 Self-hosted'],
    // DVD-style position & velocity
    x: Math.random() * (window.innerWidth - 100),
    y: Math.random() * (window.innerHeight - 100),
    vx: (Math.random() - 0.5) * 2, // velocity X
    vy: (Math.random() - 0.5) * 2, // velocity Y
    rotation: 0
  },
  {
    name: 'PocketBase',
    emoji: '📦',
    color: '#0090FF',
    description: 'Один Go файл = весь бекенд. SQLite, realtime, auth - все включено.',
    features: [
      'Single executable file',
      'SQLite database',
      'Admin dashboard included'
    ],
    tags: ['#Go', '#SQLite', '#Portable'],
    stats: ['⭐ 30k+ Stars', '💰 Free', '🌐 pocketbase.io', '🚀 5 min setup'],
    x: Math.random() * (window.innerWidth - 100),
    y: Math.random() * (window.innerHeight - 100),
    vx: (Math.random() - 0.5) * 2,
    vy: (Math.random() - 0.5) * 2,
    rotation: 0
  },
  {
    name: 'Appwrite',
    emoji: '🏗️',
    color: '#FD366E',
    description: 'Self-hosted Firebase альтернатива. Docker контейнер - і ти володар свого бекенду.',
    features: [
      'Self-hosted solution',
      'Multiple SDKs',
      'Database, auth, storage'
    ],
    tags: ['#Self-hosted', '#Docker', '#Privacy'],
    stats: ['⭐ 40k+ Stars', '💰 Free', '🌐 appwrite.io', '🔒 Full control'],
    x: Math.random() * (window.innerWidth - 100),
    y: Math.random() * (window.innerHeight - 100),
    vx: (Math.random() - 0.5) * 2,
    vy: (Math.random() - 0.5) * 2,
    rotation: 0
  },
  {
    name: 'AWS Amplify',
    emoji: '☁️',
    color: '#FF9900',
    description: 'AWS екосистема для фронтенд девів. Потужно, але складно.',
    features: [
      'Full AWS ecosystem',
      'GraphQL API',
      'Enterprise ready'
    ],
    tags: ['#AWS', '#Enterprise', '#GraphQL'],
    stats: ['⭐ 9k+ Stars', '💰 Pay as go', '🌐 aws.amazon.com', '🏢 Enterprise'],
    x: Math.random() * (window.innerWidth - 100),
    y: Math.random() * (window.innerHeight - 100),
    vx: (Math.random() - 0.5) * 2,
    vy: (Math.random() - 0.5) * 2,
    rotation: 0
  },
  {
    name: 'Railway',
    emoji: '🚂',
    color: '#8B5CF6',
    description: 'Deploy будь-чого за 5 хвилин. Git push - і все працює.',
    features: [
      'Instant deployments',
      'Multiple languages',
      'Simple pricing'
    ],
    tags: ['#Deploy', '#Simple', '#Fast'],
    stats: ['⭐ 3k+ Stars', '💰 $5/month', '🌐 railway.app', '⚡ 5 min deploy'],
    x: Math.random() * (window.innerWidth - 100),
    y: Math.random() * (window.innerHeight - 100),
    vx: (Math.random() - 0.5) * 2,
    vy: (Math.random() - 0.5) * 2,
    rotation: 0
  },
  {
    name: 'Render',
    emoji: '🎨',
    color: '#6366F1',
    description: 'Heroku, але сучасний і швидший. Web services + databases.',
    features: [
      'Static sites & APIs',
      'PostgreSQL included',
      'Auto SSL & CDN'
    ],
    tags: ['#Modern', '#Fast', '#SSL'],
    stats: ['⭐ Growing', '💰 Free tier', '🌐 render.com', '🌍 Global CDN'],
    x: Math.random() * (window.innerWidth - 100),
    y: Math.random() * (window.innerHeight - 100),
    vx: (Math.random() - 0.5) * 2,
    vy: (Math.random() - 0.5) * 2,
    rotation: 0
  },
  {
    name: 'Xata',
    emoji: '🗃️',
    color: '#8B5CF6',
    description: 'PostgreSQL + повнотекстовий пошук з коробки. TypeScript first.',
    features: [
      'PostgreSQL + search',
      'TypeScript SDK',
      'Branching workflows'
    ],
    tags: ['#PostgreSQL', '#Search', '#TypeScript'],
    stats: ['⭐ New', '💰 Free tier', '🌐 xata.io', '🔍 Full-text'],
    x: Math.random() * (window.innerWidth - 100),
    y: Math.random() * (window.innerHeight - 100),
    vx: (Math.random() - 0.5) * 2,
    vy: (Math.random() - 0.5) * 2,
    rotation: 0
  },
  {
    name: 'Nhost',
    emoji: '🎯',
    color: '#0052CC',
    description: 'Firebase для GraphQL lovers. Hasura + PostgreSQL + auth.',
    features: [
      'GraphQL API',
      'PostgreSQL + Hasura',
      'Authentication built-in'
    ],
    tags: ['#GraphQL', '#Hasura', '#PostgreSQL'],
    stats: ['⭐ 7k+ Stars', '💰 Free tier', '🌐 nhost.io', '📊 GraphQL'],
    x: Math.random() * (window.innerWidth - 100),
    y: Math.random() * (window.innerHeight - 100),
    vx: (Math.random() - 0.5) * 2,
    vy: (Math.random() - 0.5) * 2,
    rotation: 0
  },
  {
    name: 'Back4App',
    emoji: '📱',
    color: '#0080FF',
    description: 'Parse Server як сервіс. Mobile-first підхід.',
    features: [
      'Parse Server managed',
      'Mobile SDKs',
      'Low-code backend'
    ],
    tags: ['#Parse', '#Mobile', '#Low-code'],
    stats: ['⭐ Parse based', '💰 Free tier', '🌐 back4app.com', '📱 Mobile first'],
    x: Math.random() * (window.innerWidth - 100),
    y: Math.random() * (window.innerHeight - 100),
    vx: (Math.random() - 0.5) * 2,
    vy: (Math.random() - 0.5) * 2,
    rotation: 0
  },
  {
    name: 'Parse',
    emoji: '🔵',
    color: '#169CEE',
    description: 'Open-source mobile backend (Facebook legacy). Self-hosted.',
    features: [
      'Open source',
      'Mobile-first',
      'Self-hosted option'
    ],
    tags: ['#OpenSource', '#Mobile', '#Legacy'],
    stats: ['⭐ 20k+ Stars', '💰 Free', '🌐 parseplatform.org', '🏛️ Proven'],
    x: Math.random() * (window.innerWidth - 100),
    y: Math.random() * (window.innerHeight - 100),
    vx: (Math.random() - 0.5) * 2,
    vy: (Math.random() - 0.5) * 2,
    rotation: 0
  }
];
```

### 2. Initialization

```javascript
const logoContainer = document.getElementById('logoContainer');
const detailCard = document.getElementById('detailCard');
const ambientGlow = document.getElementById('ambientGlow');

let currentIndex = 0;
let isColliding = false;
const COLLISION_INTERVAL = 4000; // 4 seconds
const LOGO_SIZE = 70;

// Create floating logos
backends.forEach((backend, index) => {
  const logo = document.createElement('div');
  logo.className = 'floating-logo';
  logo.textContent = backend.emoji;
  logo.style.color = backend.color;
  logo.dataset.index = index;
  backend.element = logo;
  backend.isColliding = false;
  
  // Initial position - random across screen
  backend.element.style.opacity = '0';
  backend.element.style.transform = `translate(${backend.x}px, ${backend.y}px) scale(0.5)`;
  
  logoContainer.appendChild(logo);
  
  // Fade in with staggered delay
  setTimeout(() => {
    backend.element.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
    backend.element.style.opacity = '1';
    backend.element.style.transform = `translate(${backend.x}px, ${backend.y}px) scale(1)`;
    
    // Remove transition after animation
    setTimeout(() => {
      backend.element.style.transition = '';
    }, 500);
  }, index * 100);
});
```

### 3. DVD-style Bouncing Animation

```javascript
// Main animation loop - DVD screensaver style
function animate() {
  backends.forEach((backend) => {
    if (backend.isColliding) return;
    
    // Update position
    backend.x += backend.vx;
    backend.y += backend.vy;
    
    // Bounce off walls
    if (backend.x <= 0 || backend.x >= window.innerWidth - LOGO_SIZE) {
      backend.vx *= -1;
      backend.x = Math.max(0, Math.min(window.innerWidth - LOGO_SIZE, backend.x));
    }
    
    if (backend.y <= 0 || backend.y >= window.innerHeight - LOGO_SIZE) {
      backend.vy *= -1;
      backend.y = Math.max(0, Math.min(window.innerHeight - LOGO_SIZE, backend.y));
    }
    
    // Breathing effect (subtle scale pulse)
    const breathe = 1 + 0.05 * Math.sin(Date.now() / 1000 * 2);
    
    // Apply transform
    backend.element.style.transform = `
      translate(${backend.x}px, ${backend.y}px)
      scale(${breathe})
    `;
    
    // Glow effect (pulsing)
    const glowIntensity = 20 + 10 * Math.sin(Date.now() / 1000 * 3);
    backend.element.style.filter = 
      `drop-shadow(0 0 ${glowIntensity}px ${backend.color})`;
  });
  
  requestAnimationFrame(animate);
}
```

### 4. Card Update Function

```javascript
function updateCard(backend) {
  const content = document.getElementById('cardContent');
  
  // Animate content change
  content.style.animation = 'none';
  setTimeout(() => {
    content.style.animation = 'contentSlide 0.5s ease-out';
  }, 10);

  // Update content
  document.getElementById('cardLogo').textContent = backend.emoji;
  document.getElementById('cardTitle').textContent = backend.name.toUpperCase();
  document.getElementById('cardDescription').textContent = backend.description;
  
  // Update features
  const featuresEl = document.getElementById('cardFeatures');
  if (featuresEl) {
    const featuresHTML = backend.features.map(f => `<li>${f}</li>`).join('');
    featuresEl.innerHTML = featuresHTML;
  }
  
  // Update tags
  const tagsEl = document.getElementById('cardTags');
  if (tagsEl) {
    const tagsHTML = backend.tags.map(t => `<span class="tag">${t}</span>`).join('');
    tagsEl.innerHTML = tagsHTML;
  }
  
  // Update stats
  const statsEl = document.querySelector('.card-stats');
  if (statsEl) {
    const statsHTML = backend.stats.map(s => `<div class="stat">${s}</div>`).join('');
    statsEl.innerHTML = statsHTML;
  }

  // Update card border color & glow
  detailCard.style.borderColor = backend.color + '40';
  detailCard.style.boxShadow = `0 8px 32px rgba(0, 0, 0, 0.3), 0 0 40px ${backend.color}40`;
  
  // Update ambient glow
  ambientGlow.style.background = `radial-gradient(circle, ${backend.color}30, transparent 70%)`;
}
```

### 5. Collision Sequence (кожні 4 секунди)

```javascript
async function collisionSequence() {
  if (isColliding) return;
  isColliding = true;

  const backend = backends[currentIndex];
  backend.isColliding = true;

  // PHASE 1: Activation (0.5s)
  backends.forEach((b, i) => {
    if (i === currentIndex) {
      b.element.classList.add('active');
    } else {
      b.element.classList.add('dimmed');
    }
  });

  await sleep(500);

  // PHASE 2: Fly to card (1s)
  const cardRect = detailCard.getBoundingClientRect();
  const targetX = cardRect.left + cardRect.width / 2 - LOGO_SIZE / 2;
  const targetY = cardRect.top + cardRect.height / 2 - LOGO_SIZE / 2;

  backend.element.style.transition = 'all 1s cubic-bezier(0.4, 0, 0.2, 1)';
  backend.element.style.transform = `translate(${targetX}px, ${targetY}px) scale(1.5)`;

  // Create trail particles
  let trailInterval = setInterval(() => {
    if (backend.isColliding) {
      const trail = document.createElement('div');
      trail.className = 'trail-particle';
      trail.textContent = backend.emoji;
      trail.style.color = backend.color;
      trail.style.left = backend.element.style.left;
      trail.style.top = backend.element.style.top;
      trail.style.transform = backend.element.style.transform;
      trail.style.opacity = '0.6';
      logoContainer.appendChild(trail);
      setTimeout(() => trail.remove(), 500);
    }
  }, 50);

  await sleep(1000);
  clearInterval(trailInterval);

  // PHASE 3: Collision Impact (0.3s)
  detailCard.style.animation = 'none';
  setTimeout(() => {
    detailCard.style.animation = 'shake 0.1s';
  }, 10);

  // Create ripple waves
  for (let i = 0; i < 3; i++) {
    setTimeout(() => {
      const ripple = document.createElement('div');
      ripple.className = 'ripple';
      ripple.style.color = backend.color;
      detailCard.appendChild(ripple);
      setTimeout(() => ripple.remove(), 600);
    }, i * 150);
  }

  // Create burst particles
  for (let i = 0; i < 25; i++) {
    setTimeout(() => {
      const particle = document.createElement('div');
      particle.className = 'burst-particle';
      particle.style.background = backend.color;
      const angle = (Math.PI * 2 * i) / 25;
      const distance = 100 + Math.random() * 100;
      particle.style.setProperty('--tx', Math.cos(angle) * distance + 'px');
      particle.style.setProperty('--ty', Math.sin(angle) * distance + 'px');
      particle.style.left = targetX + LOGO_SIZE / 2 + 'px';
      particle.style.top = targetY + LOGO_SIZE / 2 + 'px';
      logoContainer.appendChild(particle);
      setTimeout(() => particle.remove(), 800);
    }, i * 10);
  }

  await sleep(300);

  // PHASE 4: Update Card Content (0.7s)
  updateCard(backend);

  await sleep(700);

  // PHASE 5: Return to floating (0.8s)
  backend.element.style.transition = 'all 0.8s ease-out';
  backend.element.style.transform = `translate(${backend.x}px, ${backend.y}px) scale(1)`;

  await sleep(800);

  // Reset everything
  backend.element.style.transition = '';
  backend.element.classList.remove('active');
  backends.forEach(b => b.element.classList.remove('dimmed'));
  backend.isColliding = false;
  isColliding = false;

  // Move to next backend
  currentIndex = (currentIndex + 1) % backends.length;
}

// Utility sleep function
function sleep(ms) {
  return new Promise(resolve => setTimeout(resolve, ms));
}
```

### 6. Start Everything

```javascript
// Initialize
animate(); // Start DVD bouncing animation
updateCard(backends[0]); // Show first backend

// Start collision sequence after 2 seconds
setTimeout(() => {
  setInterval(collisionSequence, COLLISION_INTERVAL);
  collisionSequence();
}, 2000);

// Add shake animation keyframes dynamically
const style = document.createElement('style');
style.textContent = `
  @keyframes shake {
    0%, 100% { transform: translate(-50%, -50%); }
    25% { transform: translate(-50%, -50%) translate(-2px, 2px); }
    50% { transform: translate(-50%, -50%) translate(2px, -2px); }
    75% { transform: translate(-50%, -50%) translate(-2px, -2px); }
  }
`;
document.head.appendChild(style);
```

---

## 🎯 Логіка анімації покроково

### Timeline одного циклу (4 секунди):

```
0.0s → Логотипи плавають (DVD bouncing)
       ↓
0.5s → Один логотип активується (glow збільшується)
       Інші стають напівпрозорими
       ↓
1.5s → Активний логотип летить до центральної картки
       Залишає trail з частинок
       ↓
1.8s → COLLISION! 
       - Screen shake
       - Ripple waves (3 хвилі)
       - Burst particles (25 частинок)
       ↓
2.5s → Картка оновлюється новим контентом
       - Logo
       - Title
       - Description
       - Features
       - Tags
       - Stats
       - Border color змінюється
       - Ambient glow оновлюється
       ↓
3.3s → Логотип повертається на свою траєкторію
       ↓
4.0s → Всі логотипи знову видимі
       Готовність до наступного циклу
       ↓
       [Repeat з наступним бекендом]
```

---

## 🔧 Параметри для налаштування

### Швидкість анімації:
```javascript
const COLLISION_INTERVAL = 4000;  // Інтервал між collision (мс)
const FLY_DURATION = 1000;        // Час польоту до картки (мс)
const RETURN_DURATION = 800;      // Час повернення (мс)
```

### Розміри:
```javascript
const LOGO_SIZE = 70;             // Розмір логотипу (px)
const CARD_WIDTH = 420;           // Ширина картки (px)
const AMBIENT_GLOW_SIZE = 600;    // Розмір ambient glow (px)
```

### Швидкість руху (DVD bouncing):
```javascript
// У data structure кожного бекенду:
vx: (Math.random() - 0.5) * 2,    // -1 до 1 px/frame
vy: (Math.random() - 0.5) * 2,    // -1 до 1 px/frame

// Для швидшого руху:
vx: (Math.random() - 0.5) * 4,    // -2 до 2 px/frame
vy: (Math.random() - 0.5) * 4,
```

### Ефекти:
```javascript
const BREATHE_AMPLITUDE = 0.05;   // Амплітуда breathing effect
const GLOW_BASE = 20;             // Базова інтенсивність glow (px)
const GLOW_PULSE = 10;            // Амплітуда пульсації glow (px)
const TRAIL_LIFETIME = 500;       // Час життя trail частинки (мс)
const BURST_PARTICLES = 25;       // Кількість burst particles
```

---

## 📱 Responsive поведінка

### Desktop (> 768px):
- Всі ефекти активні
- Картка: 420px ширина
- Логотипи: 70px
- CTA картка видима

### Tablet (≤ 768px):
- Картка: 90% ширини екрану
- Логотипи: 50px
- Спрощені ефекти (менше particles)
- CTA картка прихована

### Mobile (≤ 480px):
- Картка: 95% ширини
- Логотипи: 40px
- Мінімальні ефекти
- Без trail particles

---

## ⚠️ Важливі моменти

### 1. Performance
```javascript
// Використовуй requestAnimationFrame для smooth animation
function animate() {
  // ... animation logic
  requestAnimationFrame(animate);
}

// Обмеж кількість DOM операцій
// Краще змінювати transform ніж top/left
backend.element.style.transform = `translate(${x}px, ${y}px)`;
```

### 2. Cleanup
```javascript
// Видаляй старі частинки щоб не засмічувати DOM
setTimeout(() => particle.remove(), 800);

// Очищуй intervals
clearInterval(trailInterval);
```

### 3. Z-index шари
```
100 - Header, CTA
50  - Detail card
49  - Ambient glow
10  - Floating logos
5   - Effects (trails, ripples, bursts)
1   - Grid background
```

### 4. Колізії з карткою
```javascript
// Логотипи не мають заходити в область картки під час bouncing
// Додай перевірку:
const cardRect = detailCard.getBoundingClientRect();
if (backend.x + LOGO_SIZE > cardRect.left && 
    backend.x < cardRect.right &&
    backend.y + LOGO_SIZE > cardRect.top && 
    backend.y < cardRect.bottom) {
  // Відштовхнути логотип від картки
}
```

---

## 🧪 Тестування

### Чеклист:
- [ ] Логотипи з'являються в рандомних місцях
- [ ] Логотипи плавно bouncing (не застрягають в кутах)
- [ ] Collision анімація працює кожні 4 секунди
- [ ] Картка оновлюється правильним контентом
- [ ] Trail particles з'являються і зникають
- [ ] Ripple і burst effects працюють
- [ ] Ambient glow міняє колір
- [ ] Логотип повертається на свою позицію
- [ ] Цикл повторюється безкінечно
- [ ] CTA картка видима справа внизу
- [ ] Responsive працює на різних екранах
- [ ] Немає memory leaks (particles cleanup)
- [ ] 60 FPS анімація (smooth)

---

## 🎨 Додаткові можливості (optional)

### 1. Pause on hover
```javascript
detailCard.addEventListener('mouseenter', () => {
  isColliding = true; // Зупинити цикл
});

detailCard.addEventListener('mouseleave', () => {
  isColliding = false; // Продовжити
});
```

### 2. Click на логотип = показати той backend
```javascript
logo.addEventListener('click', () => {
  if (!isColliding) {
    currentIndex = index;
    collisionSequence();
  }
});
```

### 3. Keyboard navigation
```javascript
document.addEventListener('keydown', (e) => {
  if (e.key === 'ArrowRight' && !isColliding) {
    currentIndex = (currentIndex + 1) % backends.length;
    collisionSequence();
  }
  if (e.key === 'ArrowLeft' && !isColliding) {
    currentIndex = (currentIndex - 1 + backends.length) % backends.length;
    collisionSequence();
  }
});
```

---

## 📝 Фінальний чеклист інтеграції

### Перед commit:
- [ ] Створено детальний план (do plan)
- [ ] Код працює без помилок
- [ ] Немає конфліктів зі існуючими стилями
- [ ] Responsive працює
- [ ] Performance OK (60 FPS)
- [ ] Cleanup працює (немає memory leaks)
- [ ] Можна легко rollback до старої версії
- [ ] Код задокументований
- [ ] Протестовано на різних браузерах

### Файли для створення/модифікації:
```
slides/
  └── Slide16.jsx (або .tsx)
      └── BackendEcosystem/
          ├── index.jsx
          ├── styles.css
          ├── animations.js
          └── data.js
```

---

## 🚀 Ready to implement!

Тепер у тебе є всі деталі для імплементації анімації.

**ВАЖЛИВО: Спершу створи план (do plan), а потім почни кодити!**

Успіхів! 🎉